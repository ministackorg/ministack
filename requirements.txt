hypercorn==0.18.0
docker>=7.0.0
cbor2>=5.4.0
# Pinned to the version the images carry (awscli==1.45.63 requires it), so a
# pip install and a Docker run classify requests against identical service models.
botocore==1.43.63
